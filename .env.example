# Public browser values from Supabase Project Settings > API.
# These are safe to use in the frontend when Row Level Security is enabled.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Never put the service-role key in this repository or in browser code.
